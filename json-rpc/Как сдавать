Для вызова каждого метода мы формируем строку типа этой
payload={  "method": "get_studio","id": 1,"params": [1], "jsonrpc": "2.0"}
У нас есть название метода,его номер и передаваемые параметры
Эти данные обрабатывает сервер и вызывает метод(если он есть) с указанными параметрами
Остальное лирика